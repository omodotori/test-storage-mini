# Document Storage Service

Мини-сервис для хранения документов с версионностью, метаданными и базовым AI-анализом.

## 🚀 Функционал

- ✅ Загрузка файлов с автоматической версионностью
- ✅ Список всех загруженных файлов
- ✅ AI-анализ документов (mock-функция)
- ✅ Получение результатов анализа
- ✅ SQLite база данных
- ✅ Pydantic схемы для валидации
- ✅ Логирование всех операций
- ✅ Docker и Docker Compose
- ✅ Автоматические тесты (pytest)

## 📋 Требования

- Python 3.8+
- pip
- Docker (опционально)

## 🛠 Установка и запуск

### Вариант 1: Обычный запуск

```bash
# 1. Клонируйте репозиторий
git clone <your-repo-url>
cd backend

# 2. Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# 3. Установите зависимости
pip install -r requirements.txt

# 4. Запустите сервер
uvicorn app.main:app --reload
```

### Вариант 2: Docker

```bash
# Сборка и запуск
docker build -t doc-storage .
docker run -p 8000:8000 doc-storage
```

### Вариант 3: Docker Compose (рекомендуется)

```bash
# Запуск одной командой
docker-compose up --build

# Остановка
docker-compose down
```

Сервис будет доступен по адресу: `http://127.0.0.1:8000`

Документация API (Swagger): `http://127.0.0.1:8000/docs`

## 📚 API Endpoints

### 1. Корневой эндпоинт
**GET** `/`

Информация о сервисе и доступных эндпоинтах.

### 2. Загрузка файла
**POST** `/files/upload`

Загружает файл и автоматически увеличивает версию при повторной загрузке файла с тем же именем.

```bash
curl -X POST "http://127.0.0.1:8000/files/upload" \
  -F "file=@plan.pdf"
```

**Ответ:**
```json
{
  "id": 1,
  "file_name": "plan.pdf",
  "version": 1,
  "size": 20000,
  "uploaded_at": "2025-12-10T10:30:00"
}
```

### 3. Список файлов
**GET** `/files`

Возвращает список всех загруженных файлов.

```bash
curl "http://127.0.0.1:8000/files"
```

**Ответ:**
```json
[
  {
    "id": 1,
    "file_name": "plan.pdf",
    "version": 1,
    "size": 20000,
    "uploaded_at": "2025-12-10T10:30:00"
  }
]
```

### 4. Анализ файла
**POST** `/files/{file_id}/analyze`

Запускает AI-анализ файла (используется mock-функция).

```bash
curl -X POST "http://127.0.0.1:8000/files/1/analyze"
```

**Ответ:**
```json
{
  "file_id": 1,
  "analysis_id": 1,
  "result": "Файл 'plan.pdf' относительно небольшой (20000 байт), новое изменение выглядит незначительным."
}
```

### 5. Получение результата анализа
**GET** `/files/{file_id}/analysis`

Возвращает сохраненный результат анализа.

```bash
curl "http://127.0.0.1:8000/files/1/analysis"
```

**Ответ:**
```json
{
  "file_id": 1,
  "file_name": "plan.pdf",
  "version": 1,
  "analysis_result": "Файл 'plan.pdf' относительно небольшой (20000 байт), новое изменение выглядит незначительным."
}
```

### 6. Информация о файле
**GET** `/files/{file_id}`

Возвращает детальную информацию о файле.

```bash
curl "http://127.0.0.1:8000/files/1"
```

## 🧪 Тестирование

Проект включает автоматические тесты на pytest.

```bash
# Запуск всех тестов
pytest

# Запуск с подробным выводом
pytest -v

# Запуск конкретного теста
pytest tests/test_main.py::test_upload_file

# Запуск с покрытием кода
pytest --cov=app tests/
```

**Покрытие тестами:**
- ✅ Загрузка файлов
- ✅ Версионность
- ✅ Получение списка файлов
- ✅ AI-анализ
- ✅ Получение результатов анализа
- ✅ Обработка ошибок (404, 422)

## 📁 Структура проекта

```
backend/
├── app/
│   ├── __init__.py          # Инициализация пакета
│   ├── main.py              # FastAPI routes и логика
│   ├── models.py            # SQLAlchemy модели
│   ├── database.py          # Подключение к БД
│   ├── schemas.py           # Pydantic схемы
│   └── storage/             # Хранилище файлов
├── tests/
│   ├── __init__.py
│   └── test_main.py         # Тесты
├── venv/
├── requirements.txt         # Зависимости Python
├── Dockerfile               # Docker образ
├── docker-compose.yml       # Docker Compose конфигурация
├── pytest.ini               # Настройки pytest
├── .gitignore
└── README.md
```

## 🔧 Технологии

- **FastAPI** - современный веб-фреймворк для Python
- **SQLAlchemy** - ORM для работы с базой данных
- **Pydantic** - валидация данных и схемы
- **SQLite** - легковесная база данных
- **pytest** - фреймворк для тестирования
- **Docker** - контейнеризация
- **uvicorn** - ASGI сервер

## ✨ Особенности реализации

### 1. Автоматическая версионность
При загрузке файла с существующим именем версия увеличивается автоматически. Все версии сохраняются.

```python
# Пример:
# Первая загрузка plan.pdf → version: 1
# Вторая загрузка plan.pdf → version: 2
# Третья загрузка plan.pdf → version: 3
```

### 2. Mock AI-анализ
Вместо реального OpenAI API используется функция-имитация, которая генерирует комментарии на основе размера файла:

- < 10KB: "очень маленький"
- < 100KB: "относительно небольшой"
- < 1MB: "среднего размера"
- > 1MB: "большой"

### 3. Pydantic схемы
Все API endpoints используют типизированные схемы для валидации входных и выходных данных.

### 4. Логирование
Все операции логируются с указанием времени, уровня и деталей операции.

### 5. Обработка ошибок
- 404 - файл или анализ не найден
- 422 - ошибка валидации данных
- 400 - неверный запрос

## 📊 Примеры использования

### Тестирование версионности

```bash
# Загрузите файл plan.pdf первый раз
curl -X POST "http://127.0.0.1:8000/files/upload" \
  -F "file=@plan.pdf"
# Результат: version: 1

# Загрузите plan.pdf снова
curl -X POST "http://127.0.0.1:8000/files/upload" \
  -F "file=@plan.pdf"
# Результат: version: 2

# Все версии доступны через GET /files
curl "http://127.0.0.1:8000/files"
```

### Полный workflow

```bash
# 1. Загрузка файла
curl -X POST "http://127.0.0.1:8000/files/upload" \
  -F "file=@document.pdf"

# 2. Получение списка файлов
curl "http://127.0.0.1:8000/files"

# 3. Анализ файла с ID=1
curl -X POST "http://127.0.0.1:8000/files/1/analyze"

# 4. Получение результата анализа
curl "http://127.0.0.1:8000/files/1/analysis"
```

### Использование через Swagger UI

1. Откройте `http://127.0.0.1:8000/docs`
2. Попробуйте все endpoints интерактивно
3. Swagger автоматически валидирует запросы

## 🔍 Логи

Сервис ведет подробные логи всех операций:

```
2025-12-10 10:30:00 - app.main - INFO - Storage directory initialized at ./storage
2025-12-10 10:30:15 - app.main - INFO - Uploading file: plan.pdf
2025-12-10 10:30:15 - app.main - INFO - File version: 1
2025-12-10 10:30:15 - app.main - INFO - File saved to ./storage/plan.pdf_v1, size: 20000 bytes
2025-12-10 10:30:15 - app.main - INFO - File record created in DB with ID: 1
```

## 🐳 Docker

### Сборка образа
```bash
docker build -t doc-storage .
```

### Запуск контейнера
```bash
docker run -p 8000:8000 \
  -v $(pwd)/storage:/app/storage \
  -v $(pwd)/files.db:/app/files.db \
  doc-storage
```

### Docker Compose
```bash
# Запуск
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down
```

## 📝 TODO / Возможные улучшения

- [ ] Переход на PostgreSQL для продакшена
- [ ] Интеграция с реальным OpenAI API
- [ ] Аутентификация и авторизация
- [ ] Скачивание файлов через API
- [ ] Удаление файлов и версий
- [ ] Поиск по файлам и метаданным
- [ ] Rate limiting
- [ ] Кеширование результатов анализа
- [ ] Асинхронная обработка больших файлов
- [ ] CI/CD pipeline

## 🤝 Контрибьюция

Pull requests приветствуются! Для крупных изменений откройте сначала issue для обсуждения.

## 📄 Лицензия

MIT

## 👤 Автор

[Ваше ФИО]

GitHub: [ваш-username]

---

**Создано как тестовое задание для демонстрации навыков работы с FastAPI, SQLAlchemy, Docker и современными практиками разработки.**