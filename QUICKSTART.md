# 🚀 Быстрый старт

## За 3 минуты

### 1️⃣ Установка
```bash
# Клонируйте репозиторий
git clone <your-repo>
cd backend

# Установите зависимости
pip install -r requirements.txt
```

### 2️⃣ Запуск
```bash
# Запустите сервер
uvicorn app.main:app --reload
```

### 3️⃣ Тестирование
Откройте браузер: `http://127.0.0.1:8000/docs`

Или через curl:
```bash
# Загрузите файл
curl -X POST "http://127.0.0.1:8000/files/upload" \
  -F "file=@your_file.pdf"

# Получите список файлов
curl "http://127.0.0.1:8000/files"
```

---

## Docker (еще быстрее)

```bash
# Одна команда для запуска
docker-compose up --build

# Готово! Открывайте http://127.0.0.1:8000/docs
```

---

## Проверка тестов

```bash
# Запуск тестов
pytest -v

# Или используйте скрипт
chmod +x run_tests.sh
./run_tests.sh
```

---

## Основные endpoints

| Метод | URL | Описание |
|-------|-----|----------|
| POST | `/files/upload` | Загрузить файл |
| GET | `/files` | Список файлов |
| POST | `/files/{id}/analyze` | Анализ файла |
| GET | `/files/{id}/analysis` | Результат анализа |

---

**Полная документация: [README.md](README.md)**